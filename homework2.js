let pizzaPlace = "pizzahut";
let numberOfToppings = 3; 

console.log(`hello welcome to ${pizzaPlace} ${typeof pizzaPlace} we had ${numberOfToppings} ${typeof numberOfToppings} toppings`);

if (numberOfToppings < 10) {
    console.log("quality not quantity"); 
    }
        else
    {
    console.log("a whole lot of pizza");
}